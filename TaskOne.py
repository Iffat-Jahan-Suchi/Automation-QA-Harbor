#Set up development environment;
# Write script to calculate rectangle area using user input;
# Experiment with string concatenation and type conversion

length=float(input("Enter length "))
width=float(input("Enter width "))

area=length*width

print(f"Rectangle area is {area}")